export class PlayNetCharacter {
    id: string;
    name: string;
}

export class PlayNetInstance {
    code: string;
    name: string;
}

export class PlayNetLoginData {
    host: string;
    port: number;
    key: string;
}
